import 'package:flutter/material.dart';

class Sections extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Sections")),
      body: Center(child: Text("Bienvenue sur la section")),
    );
  }
}
