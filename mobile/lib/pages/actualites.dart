import 'package:flutter/material.dart';

class Actualites extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Actualites")),
      body: Center(child: Text("Bienvenue sur l'actualité")),
    );
  }
}
