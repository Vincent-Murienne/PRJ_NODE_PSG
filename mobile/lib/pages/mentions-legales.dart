import 'package:flutter/material.dart';

class MentionsLegales extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mentions légales")),
      body: Center(child: Text("Bienvenue sur la page de mentions légales")),
    );
  }
}
