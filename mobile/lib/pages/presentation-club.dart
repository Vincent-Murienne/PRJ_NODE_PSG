import 'package:flutter/material.dart';

class PresentationClub extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Presentation")),
      body: Center(child: Text("Bienvenue sur la presentation")),
    );
  }
}
